The given text is in Python, not another language that needs translation. It appears to be importing modules from within the same package. The code would remain the same in English.

Here's a breakdown of what each line does:

1. `from .actions import *`: This line imports all publicly accessible objects (functions, classes, variables) from the `actions` module located in the current package (`.`).
2. `from .navigation import *`: This line imports all publicly accessible objects from the `navigation` module.
3. `from .pagination import *`: This line imports all publicly accessible objects from the `pagination` module.
4. `from .page import *`: This line imports all publicly accessible objects from the `page` module.

The dot (.) before each module name indicates that these modules are located in the same package as the current script.

