Here is the translation of the given text to English, keeping the code unchanged:

```
@echo off
python bot.py
pause
```

The code is a Windows batch script that uses Python. It does the following:

1. `@echo off`: This line turns off the command echoing in the batch file, which means that only the final output will be displayed in the command prompt.
2. `python bot.py`: This line runs the Python script named "bot.py" using the Python interpreter.
3. `pause`: This line pauses the execution of the batch file and displays "Press any key to continue..." until a key is pressed.

